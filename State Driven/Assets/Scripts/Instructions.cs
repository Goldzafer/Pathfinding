using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Instructions : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("The AI passively moves to random spots on the map. Move the blue cube close to the AI to make the AI chase the cube. Switch the 'Night Time' bool to True to make the AI move to its Sleep Node");
    }

}
